# WireTrust Backend Configuration

# Private key for signing transactions (deployer wallet)
PRIVATE_KEY=

# WireFluid RPC endpoint (fallback is optional - auto-switches on failure)
WIREFLUID_RPC=https://evm.wirefluid.com
WIREFLUID_RPC_FALLBACK=
WIREFLUID_WSS=wss://ws.wirefluid.com
WIREFLUID_CHAIN_ID=92533

# Protocol wallets
PROTOCOL_TREASURY=
ORACLE_WALLET=
FRANCHISE_WALLET=

# Server port (defaults to 3001)
PORT=3001

# CORS — comma-separated allowed origins (defaults to localhost if not set)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# PostgreSQL connection string
DATABASE_URL=postgres://user:password@host:5432/wiretrust
