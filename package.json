{
  "name": "wiretrust-protocol",
  "version": "1.0.0",
  "description": "WireTrust — Multi-franchise fan economy protocol on WireFluid",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:wirefluid": "hardhat run scripts/deploy.js --network wirefluid",
    "seed:local": "hardhat run scripts/seed.js",
    "seed:wirefluid": "hardhat run scripts/seed.js --network wirefluid"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "pg": "^8.20.0"
  }
}
